# Normalize to LF in the repo and on checkout — this project runs in a
# Linux container, so keep line endings consistent everywhere.
* text=auto eol=lf

# Binary files — never touch line endings
*.docx binary
*.png binary
*.jpg binary
*.pdf binary
